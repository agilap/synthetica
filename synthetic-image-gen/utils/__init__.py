"""
utils/__init__.py — Public surface of the utils package.

Re-exports fingerprint extraction and similarity helpers so callers can
import directly from `utils` rather than from each sub-module.
"""
